{
  "name": "cors-simple-proxy",
  "version": "1.0.0",
  "description": "Because of [The Internet](https://en.wikipedia.org/wiki/Phishing), we can't have nice things. This is a simple configuration for the cors-anywhere npm package to proxy traffic and provide the necessary HTTP CORS headers.",
  "main": "index.js",
  "dependencies": {
    "cors-anywhere": "^0.4.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
